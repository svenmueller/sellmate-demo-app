a(href="/products/#{product.id}")
	div.product
		div.image
			img.product(src="#{product.images[0].url}&size=150")
		div.info
			#{product.title}
		div.price
			- if (product.price_varies)
				#{product.price_min} to #{product.price_max}
			- else
				#{product.price_min}
				